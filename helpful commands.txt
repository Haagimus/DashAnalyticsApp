[Local package installation]
pip install -r requirements.txt --no-index --find-links file:///tmp/packages

[Alembic]
alembic stamp head (fixes ERROR [alembic.util.messaging] Target database is not
 up to date.)
alembic upgrade head (applies the latest alembic migration script)
alembic revision --autogenerate -m 'comment goes here' ( generates an update
script based on differences between the database and model classes)
